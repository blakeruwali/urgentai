version: '3.8'
services:
  app:
    build: .
    container_name: preview-1754513458939
    ports:
      - "4007:4007"
    environment:
      - PORT=4007
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
    networks:
      - preview-network

networks:
  preview-network:
    driver: bridge